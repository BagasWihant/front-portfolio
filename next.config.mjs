/** @type {import('next').NextConfig} */
const nextConfig = {
  
    images: {
      domains: ['admin-portfolio.wihant.com',
        'images.unsplash.com',
      ],
    },
  
};

export default nextConfig;
